---
- name: Preflight (tiles.mbtiles present and readable)
  ansible.builtin.import_tasks: preflight.yml

- name: Render manifests and kubectl apply
  ansible.builtin.import_tasks: deploy.yml

- name: Verify, export ca.crt, print Kibana settings
  ansible.builtin.import_tasks: verify.yml
